import React from "react";
import "./SearchBar.scss";

const SearchBar: React.FC = () => {
  return (
    <div className="search-bar">{/* Your search bar content goes here */}</div>
  );
};

export default SearchBar;
